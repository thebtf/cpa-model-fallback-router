# Onboarding Report - 2026-06-27

Created standalone project structure for `cpa-model-fallback-router`.

## Created

- Root Go module layout.
- `.agent/` coordination directories.
- `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`.
- Redoc docs scaffold under `docs/`.
- Release workflow scaffold under `.github/workflows/`.
- Local build scripts under `scripts/`.

## Source

Extracted from CPA worktree commit `35b46f23` and omitted generated `.so`, `.dll`, and `.h` artifacts.
