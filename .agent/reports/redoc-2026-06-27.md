# Redoc Documentation Pass - 2026-06-27

## Scope

Ran the `nvmd-platform:docs --redoc` documentation pass after publishing `v0.1.1` and opening the CPA Plugins Store PR.

## Source-Backed Claims

- README installation and release asset naming now match the CPA Plugins Store installer contract observed in `router-for-me/CLIProxyAPI-Plugins-Store`.
- README compatibility notes keep the known CPA plugin host limitation: selected auth/provider metadata is not exposed to plugin executors, so provider auth-kind scoping is not implemented here.
- README commands match repository scripts: `go test ./...`, `scripts/package-release.ps1`, and `go build -buildmode=c-shared`.
- Redoc source remains `docs/openapi.yaml` with version `0.1.1`.

## Walkthrough

1. Install path starts at the latest GitHub release.
2. Operator verifies the platform zip against `checksums.txt`.
3. Operator extracts one root-level dynamic library into CPA's plugin directory.
4. Operator enables `plugins.configs.model-fallback-router`.
5. CPA executes the requested model first, then fallback model names when a rule and fallback policy allow retry.

## Verification

- `go test ./...`: passed.
- `scripts/package-release.ps1 -Version 0.1.1 -GOOS windows -GOARCH amd64`: passed; produced `dist/model-fallback-router_0.1.1_windows_amd64.zip` with `model-fallback-router.dll` at archive root.
- `git diff --check`: passed; only Windows line-ending notices were printed.
- Secret/local-path scan over README, contributing docs, changelog, docs, workflows, scripts, and `.agent`: no non-whitelisted matches. The only whitelisted hit was the standard GitHub Actions `${{ github.token }}` expression.
- External link check over README and continuity/report links: all checked URLs returned HTTP 200.

## Quality Score

Score: A-

The docs cover quick start, configuration, command reference, architecture, troubleshooting, release packaging, Redoc source location, contribution workflow, link health, and secret-scan status. The remaining limitation is upstream: CPA does not yet expose selected auth/provider metadata to plugin executors, so the docs explicitly avoid promising auth-kind scoped fallback.

## Evidence To Refresh Before Future Releases

- `go test ./...`
- one `scripts/package-release.ps1` local package smoke
- GitHub release workflow status
- release asset names plus `checksums.txt`
- CPA Plugins Store PR or registry state
